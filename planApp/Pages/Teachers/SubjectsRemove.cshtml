@page
@model planApp.Pages.Teachers.SubjectsRemoveModel

@{
    ViewData["Title"] = "Usuń przedmiot";
}

<h2>Usuń przedmiot</h2>
<h3>Czy na pewno chcesz usunąć ten przedmiot?</h3>
<div>
    <h4>Przedmiot</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.Subject.Name)
        </dt>
        <dd>
            @Html.DisplayFor(model => model.Subject.Name)
        </dd>
    </dl>

    <form method="post">
        <input type="hidden" asp-for="Subject.ID" />
        <input type="hidden" asp-for="TeacherID" />
        <input type="submit" value="Usuń" class="btn btn-default" /> |
        <a asp-page="./Edit" asp-route-id="@Model.TeacherID">Wróć</a>
    </form>
</div>

