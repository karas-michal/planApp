@page
@model planApp.Pages.Teachers.AvailablePeriodsCreateModel

@{
    ViewData["Title"] = "Create";
}

<h2>Create</h2>

<h4>AvailablePeriod</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="TeacherID" />
            <div class="form-group">
                <label asp-for="AvailablePeriod.Day" class="control-label"></label>
                <input asp-for="AvailablePeriod.Day" class="form-control" />
                <span asp-validation-for="AvailablePeriod.Day" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="AvailablePeriod.Period" class="control-label"></label>
                <input asp-for="AvailablePeriod.Period" class="form-control" />
                <span asp-validation-for="AvailablePeriod.Period" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-default" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-page="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
